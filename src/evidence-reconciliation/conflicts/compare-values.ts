import type { AccountFact,FactValue } from "@/domain/facts/types";import { reconciliationPolicy } from "../config";
const synonyms:Record<string,string>={"in progress":"in-progress",pipeline:"pipeline",discovery:"discovery",scheduled:"scheduled",complete:"completed",completed:"completed"};
const normalized=(v:string)=>synonyms[v.trim().toLowerCase().replaceAll("_"," ")]??v.trim().toLowerCase().replace(/[^a-z0-9]+/g," ");
export function factsEquivalent(a:AccountFact,b:AccountFact){if(a.valueType==="date"&&typeof a.value==="string"&&typeof b.value==="string")return Math.abs(Date.parse(a.value)-Date.parse(b.value))<=reconciliationPolicy.dateToleranceMs;if(typeof a.value==="number"&&typeof b.value==="number"){const base=Math.max(Math.abs(a.value),Math.abs(b.value),1),tolerance=a.valueType==="percentage"?reconciliationPolicy.numericTolerance.percentage:reconciliationPolicy.numericTolerance.money;return Math.abs(a.value-b.value)/base<=tolerance;}if(typeof a.value==="string"&&typeof b.value==="string")return normalized(a.value)===normalized(b.value);if(Array.isArray(a.value)&&Array.isArray(b.value)){const left=[...a.value].map(normalized).sort(),right=[...b.value].map(normalized).sort();return left.length===right.length&&left.every((v,i)=>v===right[i]);}return a.value===b.value;}
export function displayFactValue(value:FactValue){return Array.isArray(value)?value.join(", "):String(value);}

