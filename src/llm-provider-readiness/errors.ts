import type {ProviderError,ProviderErrorCategory} from "@/domain/provider-readiness/types";
export function providerError(code:string,category:ProviderErrorCategory,message:string,now=()=>new Date().toISOString(),details:Record<string,string|number|boolean>={}):ProviderError{return{code,category,message,retryable:["timeout","rate-limit","provider-unavailable","transient"].includes(category),cancelled:category==="cancellation",timeout:category==="timeout",rateLimited:category==="rate-limit",authenticationFailed:category==="authentication",authorizationFailed:category==="authorization",capabilityMismatch:category==="capability",configurationError:category==="configuration",details:sanitizeDiagnostics(details),occurredAt:now()};}
const secretPattern=/(authorization|api[-_]?key|token|secret|credential|password)/i;
export function sanitizeDiagnostics(input:Record<string,unknown>):Record<string,string|number|boolean>{return Object.fromEntries(Object.entries(input).map(([key,value])=>[key,secretPattern.test(key)?"[REDACTED]":typeof value==="string"||typeof value==="number"||typeof value==="boolean"?value:String(value)]));}
