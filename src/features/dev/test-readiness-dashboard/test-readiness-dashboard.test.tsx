import {render,screen} from "@testing-library/react";import userEvent from "@testing-library/user-event";import {describe,expect,it} from "vitest";import {runPlatformReadinessSuite} from "@/test-readiness";import {TestReadinessDashboard} from "./test-readiness-dashboard";
describe("TestReadinessDashboard",()=>{it("renders coverage, resilience, demo history and blockers",async()=>{const result=await runPlatformReadinessSuite({scenarioIds:["coinbase-stable-healthy","coinbase-champion-leaves"]}),user=userEvent.setup();render(<TestReadinessDashboard matrix={result.matrix} faultRuns={result.faultRuns} demoRuns={result.demoRuns} history={result.history} summary={result.summary}/>);expect(screen.getByRole("heading",{name:"Platform Test Readiness"})).toBeInTheDocument();expect(screen.getByText("NOT READY")).toBeInTheDocument();await user.click(screen.getByRole("button",{name:"Coverage Matrix"}));expect(screen.getByRole("table")).toBeInTheDocument();await user.click(screen.getByRole("button",{name:"Fault Runs"}));expect(screen.getByRole("heading",{name:"Resilience Lab"})).toBeInTheDocument();await user.click(screen.getByRole("button",{name:"Demo Run History"}));expect(screen.getByText(/consecutive successful runs/)).toBeInTheDocument();});});
