import {render,screen} from "@testing-library/react";import userEvent from "@testing-library/user-event";import {describe,expect,it,vi} from "vitest";vi.mock("@/app/dev/provider-readiness/actions",()=>({runRealCoinbaseRenewalAction:vi.fn()}));import {ProviderReadinessInspector} from "./provider-readiness-inspector";
describe("ProviderReadinessInspector",()=>{it("renders fail-closed readiness state without credentials",()=>{render(<ProviderReadinessInspector/>);expect(screen.getByRole("heading",{name:"Provider Readiness Inspector"})).toBeVisible();expect(screen.getByRole("region",{name:"Readiness summary"})).toHaveTextContent("Blocked");expect(screen.queryByText(/api[-_ ]?key/i)).not.toBeInTheDocument();});it("runs conformance and cancellation simulations",async()=>{render(<ProviderReadinessInspector/>);await userEvent.click(screen.getByRole("button",{name:"Run provider conformance suite"}));expect(screen.getByText(/credentialsRedacted/)).toBeVisible();await userEvent.selectOptions(screen.getByLabelText("Simulation"),"cancellation");expect(screen.getByText(/cancelled safely/)).toBeVisible();});});
