import Link from "next/link";import type { AccountDigitalTwin } from "@/domain/accounts/account-digital-twin";
const money=(v:number)=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",notation:"compact",maximumFractionDigits:1}).format(v);const date=(v:string)=>new Intl.DateTimeFormat("en-US",{month:"short",day:"numeric",year:"numeric"}).format(new Date(v));
export function AccountsList({twins}:{twins:AccountDigitalTwin[]}){return <section><header className="accounts-header"><div><span className="eyebrow">ACCOUNT DIGITAL TWINS</span><h1>Enterprise Accounts</h1><p>Authoritative commercial, relationship, product and decision context.</p></div><div className="avatar">UK</div></header><div className="accounts-grid">{twins.map((twin)=><Link className="account-card" href={`/accounts/${twin.accountId}`} key={twin.accountId}><div className="account-card-head"><div><span>Tier {twin.identity.strategicTier} · {twin.identity.segment}</span><h2>{twin.identity.name}</h2><p>{twin.identity.ownerName}</p></div><span className={`health-badge health-${twin.health.overallStatus}`}>{twin.health.overallScore} · {twin.health.overallStatus}</span></div><dl><div><dt>Annual contract</dt><dd>{money(twin.commercialProfile.annualContractValue)}</dd></div><div><dt>Renewal</dt><dd>{date(twin.commercialProfile.renewalDate)}</dd></div><div><dt>Open pipeline</dt><dd>{money(twin.commercialProfile.openPipelineValue)}</dd></div><div><dt>Health trend</dt><dd>{twin.health.trend}</dd></div><div><dt>Active risks</dt><dd>{twin.knownRisks.length}</dd></div><div><dt>Active decisions</dt><dd>{twin.activeDecisions.length}</dd></div></dl><p className="last-activity">Last activity · {twin.recentActivities[0]?.title??"No recent activity"}</p></Link>)}</div></section>}
