import {describe,expect,it} from "vitest";import {createCoinbaseQualityFixture} from "@/data/ai-quality-fixtures/coinbase-renewal";
describe("Coinbase AI quality report",()=>{it("generates a complete promotion report from the single approved scenario",()=>{const{report}=createCoinbaseQualityFixture();expect(report.scenario.id).toBe("ai-quality-coinbase-renewal");expect(report.samples).toHaveLength(1);expect(report.metrics).toHaveLength(14);expect(report.baselineComparisons).toHaveLength(1);expect(report.approvedRecordings).toHaveLength(1);expect(report.promotionDecision.outcome).toBe("Conditionally Approved");expect(report.promotionDecision.preferredBaselineEligible).toBe(false);expect(report.summary).toContain("Coinbase Renewal Agent");});});
