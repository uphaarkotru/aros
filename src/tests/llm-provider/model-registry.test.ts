import {describe,expect,it} from "vitest";import {createModelRegistry,defaultModelRegistry,getModel} from "@/llm-provider";import {createProviderTestSetup} from "./test-helpers";
describe("LLM Model Registry",()=>{it("registers synthetic provider-neutral models",()=>expect(defaultModelRegistry.models.map(item=>`${item.provider}/${item.modelId}`)).toEqual(expect.arrayContaining(["mock/aros-mock-v1","replay/fixture-v1","failure/failure-v1"])));it("rejects duplicate provider and model IDs",()=>expect(()=>createModelRegistry([defaultModelRegistry.models[0],defaultModelRegistry.models[0]])).toThrow(/duplicate-model/));it("selects the provider default",()=>expect(getModel({provider:"mock",registry:defaultModelRegistry}).model?.modelId).toBe("aros-mock-v1"));it("rejects unsupported models",()=>expect(getModel({provider:"mock",modelId:"missing",registry:defaultModelRegistry}).errors[0].code).toBe("unsupported-model"));it("validates model token limits",()=>{const{request}=createProviderTestSetup();expect(getModel({provider:"mock",request:{...request,maxTokens:99999},registry:defaultModelRegistry}).errors[0].field).toBe("maxTokens")});it("blocks experimental models by default",()=>expect(getModel({provider:"failure",registry:defaultModelRegistry}).errors[0].code).toBe("unsupported-model"))});
