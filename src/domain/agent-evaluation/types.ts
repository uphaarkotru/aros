import type {AgentType} from "@/domain/agents/types";
import type {DecisionCandidate} from "@/domain/decisions/types";
import type {AgentReasoningAuditEvent,AgentReasoningMetadata,AgentReasoner,AgentReasoningResult} from "@/domain/agent-reasoning/types";
import type {AgentContextPackage} from "@/domain/llm-context/types";

export type EvaluationCategory="schema-compliance"|"account-consistency"|"evidence-citation-validity"|"claim-grounding"|"financial-impact-grounding"|"traceability"|"freshness-awareness"|"conflict-awareness"|"agent-scope"|"recommendation-specificity"|"recommendation-actionability"|"unsupported-certainty"|"hallucination-resistance";
export type EvaluationSeverity="info"|"warning"|"error"|"critical";
export interface EvaluationFinding{id:string;category:EvaluationCategory;severity:EvaluationSeverity;code:string;message:string;claimId?:string;fieldPath?:string;evidenceIds:string[];factIds:string[];conflictIds:string[];suggestedRemediation:string;blocking:boolean;}
export interface EvaluationCategoryResult{category:EvaluationCategory;score:number;passed:boolean;weight:number;findings:EvaluationFinding[];explanation:string;}
export interface HallucinationFinding{findingId:string;type:string;severity:EvaluationSeverity;text:string;claimId?:string;unsupportedValue:string;expectedContext:string;blocking:boolean;explanation:string;}
export interface TraceabilitySummary{materialClaimCount:number;fullyTraceableClaimCount:number;partiallyTraceableClaimCount:number;untraceableClaimCount:number;coverageScore:number;missingLinks:string[];explanation:string;}
export interface GroundingSummary{materialClaimCount:number;groundedClaimCount:number;partiallyGroundedClaimCount:number;unsupportedClaimCount:number;coverageScore:number;explanation:string;}
export type EvaluationVerdict="pass"|"pass-with-warnings"|"fail"|"review-required"|"unable-to-evaluate";
export type EvaluationRiskLevel="low"|"medium"|"high"|"critical";
export interface AgentEvaluationDiagnostics{categoryCount:number;findingCount:number;blockingFindingCount:number;citationCount:number;validCitationCount:number;evaluationDurationMs:number;}
export interface AgentEvaluationResult{evaluationId:string;candidateId:string;contextId:string;accountId:string;agentType:AgentType;evaluatedAt:string;policyVersion:string;overallScore:number;verdict:EvaluationVerdict;eligibleForGovernance:boolean;categoryResults:EvaluationCategoryResult[];findings:EvaluationFinding[];unsupportedClaims:EvaluationFinding[];hallucinationFindings:HallucinationFinding[];missingCitations:EvaluationFinding[];staleEvidenceFindings:EvaluationFinding[];conflictAwarenessFindings:EvaluationFinding[];scopeFindings:EvaluationFinding[];recommendationFindings:EvaluationFinding[];traceabilitySummary:TraceabilitySummary;groundingSummary:GroundingSummary;riskLevel:EvaluationRiskLevel;diagnostics:AgentEvaluationDiagnostics;auditEvents:AgentReasoningAuditEvent[];metadata:{reasoning:AgentReasoningMetadata;evaluationSchemaVersion:string;};}
export interface EvaluationPolicy{version:string;weights:Record<EvaluationCategory,number>;thresholds:{pass:number;passWithWarnings:number;reviewRequired:number};numericTolerance:number;dateToleranceDays:number;minimumCriticalCitations:number;blockingFindingCodes:string[];allowedDecisionTypes:Record<AgentType,string[]>;externalExecutionPhrases:string[];}
export type EligibilityNextStep="send-to-decision-control"|"human-review"|"retry-reasoning"|"reject"|"quarantine";
export interface CandidateEligibilityResult{eligible:boolean;reason:string;blockingFindings:EvaluationFinding[];requiredReview:boolean;allowedNextStep:EligibilityNextStep;}
export interface EvaluatedDecisionCandidate{candidate:DecisionCandidate;evaluation:AgentEvaluationResult;eligibility:CandidateEligibilityResult;}
export interface ExpectedEvaluationOutcome{expectedVerdict:EvaluationVerdict;expectedEligibility:boolean;expectedMinimumScore:number;expectedMaximumScore?:number;requiredFindingCodes:string[];forbiddenFindingCodes:string[];expectedDecisionType?:string;expectedEvidenceIds?:string[];}
export interface EvaluationScenario{id:string;name:string;description:string;accountId:string;agentType:AgentType;task:AgentContextPackage["task"];contextFixtureId:string;reasonerFixtureId:string;expectedOutcome:ExpectedEvaluationOutcome;tags:string[];version:string;}
export interface EvaluationScenarioResult{scenario:EvaluationScenario;reasoning:AgentReasoningResult;evaluation?:AgentEvaluationResult;eligibility?:CandidateEligibilityResult;passedExpectation:boolean;expectationFailures:string[];}
export interface EvaluationRegressionResult{scenarioId:string;baselineVersion:string;currentVersion:string;baselineScore:number;currentScore:number;scoreDelta:number;baselineVerdict:EvaluationVerdict;currentVerdict:EvaluationVerdict;newFindings:string[];resolvedFindings:string[];regressed:boolean;explanation:string;}
export interface EvaluationRunReport{runId:string;startedAt:string;completedAt:string;scenarioCount:number;passedCount:number;warningCount:number;reviewRequiredCount:number;failedCount:number;unableToEvaluateCount:number;averageScore:number;groundingPassRate:number;traceabilityPassRate:number;hallucinationRate:number;eligibilityRate:number;regressions:EvaluationRegressionResult[];scenarioResults:EvaluationScenarioResult[];metadata:{policyVersion:string;scenarioVersion:string;reasonerIds:string[];};}
export interface RunEvaluationScenarioInput{scenario:EvaluationScenario;context:AgentContextPackage;reasoner:AgentReasoner;policy:EvaluationPolicy;now:string;}
