import type { AccountSnapshot, SnapshotChange } from "@/domain/accounts/account-digital-twin";
export function compareSnapshots(previous:AccountSnapshot|undefined,current:AccountSnapshot):SnapshotChange[]{if(!previous)return[];const fields:[keyof AccountSnapshot,string,boolean][]=[["healthScore","Health score",true],["relationshipScore","Relationship score",true],["usageScore","Product usage",true],["renewalLikelihood","Renewal likelihood",true],["openPipelineValue","Open pipeline",true],["meddpiccCompleteness","MEDDPICC completeness",true],["activeRiskCount","Active risks",false]];return fields.flatMap(([field,name,positiveUp])=>{const before=previous[field],after=current[field];if(typeof before!=="number"||typeof after!=="number"||before===after)return[];const delta=after-before;return [{field,previous:before,current:after,delta,summary:`${name} ${delta>0?"increased":"fell"} by ${Math.abs(delta)}${field==="openPipelineValue"?"":" points"}.`,severity:(delta>0)===positiveUp?"positive" as const:"negative" as const}];});}
