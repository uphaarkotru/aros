import type { HealthStatus, Stakeholder, StakeholderMap } from "@/domain/accounts/account-digital-twin";
export function buildStakeholderMap(items:Stakeholder[]):StakeholderMap{const by=(role:Stakeholder["roleInBuyingProcess"])=>items.filter((i)=>i.roleInBuyingProcess===role);return {economicBuyer:by("economic-buyer")[0],champion:by("champion")[0],executiveSponsor:by("executive-sponsor")[0],blockers:items.filter((i)=>i.isBlocker),technicalEvaluators:by("technical-buyer"),procurement:by("procurement"),legal:by("legal"),security:by("security"),endUsers:by("user"),missingRoles:(['economic-buyer','champion','executive-sponsor'] as const).filter((role)=>!by(role).length)};}
export function relationshipScore(items:Stakeholder[]):number{if(!items.length)return 0;const values={strong:100,healthy:78,weakening:52,"at-risk":28,inactive:15,unknown:35};return Math.round(items.reduce((s,i)=>s+values[i.relationshipStrength],0)/items.length);}
export function roleHealth(item:Stakeholder|undefined):HealthStatus{return !item?"unknown":item.relationshipStrength==="strong"||item.relationshipStrength==="healthy"?"healthy":item.relationshipStrength==="weakening"?"watch":item.relationshipStrength==="at-risk"?"at-risk":"critical";}
