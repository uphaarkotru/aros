import {requireOrganizationAdmin} from "@/auth/admin-guards.server";import {identityRepository} from "@/auth/repository.server";
export default async function AdminOverview(){const identity=await requireOrganizationAdmin(),s=identityRepository.read(),org=identity.organization.id,checks=[['Users configured',s.memberships.some(x=>x.organizationId===org&&x.status==='ACTIVE')],['Roles configured',s.organizationRoles.some(x=>x.organizationId===org&&x.isActive)],['Organization structure configured',s.organizationalUnits.some(x=>x.organizationId===org)],['Reporting relationships configured',s.relationships.some(x=>x.organizationId===org&&x.relationshipType==='REPORTS_TO')],['Revenue teams configured',s.revenueTeamAssignments.some(x=>x.organizationId===org)],['Methodology selected',Boolean(identity.organization.defaultMethodology)],['CRM configured',s.integrations.some(x=>x.organizationId===org&&x.category==='CRM'&&x.status==='CONNECTED')],['Governance reviewed',s.governanceConfigurations.some(x=>x.organizationId===org)]];return <main className="admin-workspace"><span className="eyebrow">ORGANIZATION ADMINISTRATION</span><h1>Configuration overview</h1><p>Deterministic readiness checks for {identity.organization.name}.</p><section className="readiness-checklist">{checks.map(([label,ready])=><article key={String(label)}><i className={ready?'ready':''}/><strong>{label}</strong><span>{ready?'Ready':'Needs configuration'}</span></article>)}</section></main>}
