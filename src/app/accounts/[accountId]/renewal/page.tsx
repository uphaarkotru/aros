import Link from "next/link";import {notFound} from "next/navigation";import {ApplicationShell} from "@/components/application-shell";import {RenewalWorkspace} from "@/features/renewal-intelligence/renewal-workspace";import {createCoinbaseRenewalWorkspaceFixture} from "@/data/renewal-intelligence-fixtures/coinbase";
export function generateStaticParams(){return[{accountId:"acct-coinbase"}];}
export default async function RenewalPage({params}:{params:Promise<{accountId:string}>}){const{accountId}=await params;if(accountId!=="acct-coinbase")notFound();const{baseline,replayWorkspace}=createCoinbaseRenewalWorkspaceFixture();return <ApplicationShell active="accounts"><RenewalWorkspace baseline={baseline} replay={replayWorkspace}/><Link className="sr-only" href="/">Return to Morning Briefing</Link></ApplicationShell>}
