import { ApplicationShell } from "@/components/application-shell";import { accountDigitalTwins,getAccountDigitalTwin } from "@/data/synthetic/final-account-digital-twins";import { AccountDetail } from "@/features/accounts/account-detail";import Link from "next/link";
export function generateStaticParams(){return accountDigitalTwins.map((twin)=>({accountId:twin.accountId}));}
export default async function AccountPage({params}:{params:Promise<{accountId:string}>}){const {accountId}=await params;const twin=getAccountDigitalTwin(accountId);return <ApplicationShell active="accounts">{twin?<AccountDetail twin={twin}/>:<section className="not-found"><h1>Account not found</h1><p>The requested Account Digital Twin is unavailable.</p><Link className="primary-button" href="/accounts">Return to accounts</Link></section>}</ApplicationShell>}
