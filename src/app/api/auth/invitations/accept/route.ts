import {NextResponse} from "next/server";import {acceptOrganizationInvitation} from "@/auth/tenant-model";import {identityRepository} from "@/auth/repository.server";import {recordAudit} from "@/auth/audit.server";
export async function POST(request:Request){let body:Record<string,unknown>;try{body=await request.json()}catch{return NextResponse.json({error:"Invalid request."},{status:400})}const result=acceptOrganizationInvitation(identityRepository,{token:String(body.token??""),firstName:String(body.firstName??""),lastName:String(body.lastName??""),password:String(body.password??"")});if(!result.ok)return NextResponse.json({error:result.error},{status:400});recordAudit(result.value.user,{event:"USER_INVITATION_ACCEPTED",resourceType:"membership",resourceId:result.value.membership.id,after:{adminRole:result.value.membership.adminRole,status:result.value.membership.status}},result.value.membership.organizationId);return NextResponse.json({ok:true,organizationId:result.value.membership.organizationId})}
