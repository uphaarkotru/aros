import{NextResponse}from'next/server';import{identityRepository}from'@/auth/repository.server';import{acceptOrganizationInvitation}from'@/auth/tenant-model';
export async function POST(request:Request){let b:Record<string,unknown>;try{b=await request.json()}catch{return NextResponse.json({error:'Invalid request.'},{status:400})}const result=acceptOrganizationInvitation(identityRepository,{token:String(b.token??''),firstName:String(b.firstName??''),lastName:String(b.lastName??''),password:String(b.password??'')});if(!result.ok)return NextResponse.json({error:result.error},{status:400});return NextResponse.json({ok:true,organizationId:result.value.membership.organizationId})}
