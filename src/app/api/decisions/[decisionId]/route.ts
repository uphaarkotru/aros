import {NextResponse} from "next/server";import {getAuthenticatedIdentity,getRawSessionUser} from "@/auth/session.server";import {identityRepository} from "@/auth/repository.server";import {authorizePermission} from "@/auth/authorization";import {permissions} from "@/auth/permissions";import {recordAudit} from "@/auth/audit.server";import {governedDecisions} from "@/features/morning-briefing/data";
const actions=["approve","edit","dismiss","snooze","execute"] as const;type Action=typeof actions[number];
export async function POST(request:Request,{params}:{params:Promise<{decisionId:string}>}){const identity=await getAuthenticatedIdentity(),actor=await getRawSessionUser();if(!identity||!actor)return NextResponse.json({error:"Unauthenticated"},{status:401});let body:unknown;try{body=await request.json()}catch{return NextResponse.json({error:"Invalid request"},{status:400})}const action=(body as Record<string,unknown>)?.action;if(typeof action!=="string"||!actions.includes(action as Action))return NextResponse.json({error:"Invalid action"},{status:400});const{decisionId}=await params,decision=governedDecisions.find(item=>item.id===decisionId);if(!decision)return NextResponse.json({error:"Not found"},{status:404});if(!identity.scope?.accountIds.includes(decision.accountId))return NextResponse.json({error:"Resource outside scope"},{status:403});const effective=identityRepository.findUserById(identity.viewUser.id)!;if(!authorizePermission(effective,permissions.recommendationApprove)&&!authorizePermission(effective,permissions.managerDecisionApprove))return NextResponse.json({error:"Forbidden"},{status:403});const recommendation=(body as Record<string,unknown>)?.recommendation;if(action==="edit"&&(typeof recommendation!=="string"||!recommendation.trim()||recommendation.length>4000))return NextResponse.json({error:"Invalid recommendation"},{status:400});const event=recordAudit(actor,{event:`decision.${action}`,resourceType:"decision",resourceId:decisionId,payload:{accountId:decision.accountId,viewAsRole:identity.viewAsRole,...(action==="edit"?{recommendation}: {})}});return NextResponse.json({ok:true,auditEvent:event})}
