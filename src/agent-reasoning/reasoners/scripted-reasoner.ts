import type {AgentReasoner,AgentReasonerResponse,AgentReasonerResponseFormat} from "@/domain/agent-reasoning/types";import type {AgentType} from "@/domain/agents/types";import {reasoningVersions,supportedAgentTypes} from "../config";

export function createScriptedReasoner({id,rawOutput,format="structured-object",supported=supportedAgentTypes}:{id:string;rawOutput:unknown|((contextId:string)=>unknown);format?:AgentReasonerResponseFormat;supported?:AgentType[]}):AgentReasoner{return{id:`scripted-${id}`,name:`Scripted ${id}`,version:"1.0.0",supportedAgentTypes:supported,supportedOutputSchemaVersions:[reasoningVersions.decisionCandidateSchemaVersion],reason(request,context):AgentReasonerResponse{if(!supported.includes(request.agentType))throw new Error("unsupported-agent");return{rawOutput:typeof rawOutput==="function"?rawOutput(context.contextId):rawOutput,format,generatedAt:request.requestedAt,metadata:{fixture:id},simulated:true,diagnostics:{steps:["returned-scripted-output"],warningCount:format==="malformed-json"||format==="partial-object"?1:0,fixtureId:id}};}};}
