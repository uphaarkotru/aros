import {mkdirSync,writeFileSync} from "node:fs";import {spawnSync} from "node:child_process";import path from "node:path";
const suite=process.argv[2]??"readiness",files={readiness:["src/tests/test-readiness"],resilience:["src/tests/fault-injection"],demo:["src/tests/demo-reliability"],"design-partner":["src/tests/test-readiness","src/tests/fault-injection","src/tests/demo-reliability"]}[suite];if(!files){console.error(`Unknown readiness suite: ${suite}`);process.exit(2);}const startedAt=new Date().toISOString(),result=spawnSync(process.execPath,["node_modules/vitest/vitest.mjs","run",...files,"--reporter=verbose"],{stdio:"inherit",env:{...process.env,AROS_LIVE_PROVIDER_TESTS:"false",AROS_OPENAI_REAL_CALLS_ENABLED:"false"}}),outcome=result.status===0?"PASS":"FAIL",report={suite,outcome,startedAt,completedAt:new Date().toISOString(),exitCode:result.status??1,developmentOnly:true,externalCallsAllowed:false,apiKeyRequired:false,promotionAuthorized:false,productionAuthorization:false};const directory=path.join(process.cwd(),".aros","test-readiness","ci");mkdirSync(directory,{recursive:true});writeFileSync(path.join(directory,`${suite}-latest.json`),JSON.stringify(report,null,2));console.log(`\nAROS ${suite} readiness: ${outcome}`);console.log(`Machine report: .aros/test-readiness/ci/${suite}-latest.json`);process.exit(report.exitCode);
