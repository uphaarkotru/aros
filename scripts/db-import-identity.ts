import {readFile} from "node:fs/promises";import {migrateIdentityStore} from "../src/auth/store-migration";import type {IdentityStore} from "../src/auth/types";import {PostgresIdentityRepository} from "../src/db/postgres-identity-repository";
async function main(){if(!process.env.DATABASE_URL)throw new Error("DATABASE_URL is required");if(!process.env.AROS_IDENTITY_IMPORT_PATH)throw new Error("AROS_IDENTITY_IMPORT_PATH is required");const parsed=JSON.parse(await readFile(process.env.AROS_IDENTITY_IMPORT_PATH,"utf8")) as IdentityStore,repository=await PostgresIdentityRepository.connect(process.env.DATABASE_URL);await repository.importStore(migrateIdentityStore(parsed));await repository.close();console.log("Imported relational identity records while preserving IDs.")}
main().catch(error=>{console.error(error);process.exitCode=1});
